# CATXT Sync — Windows App dependencies
# Install with:  pip install -r requirements_app.txt
# Then run:      playwright install msedge

playwright>=1.40.0
requests>=2.31.0
pystray>=0.19.4
Pillow>=10.0.0
plyer>=2.1.0
pyinstaller>=6.0.0
pywin32>=306
